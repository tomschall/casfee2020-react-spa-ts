import { makeStyles } from '@material-ui/core/styles';

export default makeStyles((theme) => ({
  root: {
    // display: 'flex',
    // flexDirection: 'row',
  },
  label: {
    padding: '10px 15px',
  },
  indicator: {
    color: '#f9cd8b',
    fontSize: '10px',
    fontWeight: 700,
  },
}));
